pub struct MdlPathData {
    pub path_count: usize,
    pub path_block_size: usize,
    pub attribute_list: Vec<String>,
    pub bone_list: Vec<String>,
    pub material_list: Vec<String>,
    pub shape_list: Vec<String>,
    pub extra_path_list: Vec<String>,
}
